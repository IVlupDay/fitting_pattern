args <- commandArgs(trailingOnly = TRUE)
#args_2 <- args[1]
nums = as.numeric(args)
print(nums)

/*
 print nums
 print "Result Kernel Regression"
 
 
#Numerical result
data <- read.csv("PATH_XXXX", sep=",", header=TRUE)
result <- ksmooth(data$x, data$y, kernel="normal", bandwidth= YYYY)


write.table(data, file = "./mix_mavg_kr/result/kernelreg_result/csv_result.csv",row.names=FALSE,, sep=",")

#Minimum Best representation
with(data, {
    plot(data$x, data$y, main="Kernel Fitting Result", pch=3, col="grey")
    lines(ksmooth(data$x, data$y, "normal", bandwidth=YYYY), col = 5)
})

# Fetch command line arguments
argValue <- commandArgs(trailingOnly = TRUE)

# Convert to numerics
nums = as.numeric(myArgs)
*/

print "Kernel Regression Result is finished \n \n \n "
print "%%%%-------------- SUCEESS FITTING --------------%%%%"








